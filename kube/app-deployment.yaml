apiVersion: apps/v1
kind: Deployment
metadata:
  name: amirta-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: amirta-app
  template:
    metadata:
      labels:
        app: amirta-app
    spec:
      containers:
        - name: amirta-app
          image: harissh77/amirtaapp:1.0
          ports:
            - containerPort: 5000
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: amirta-db-config
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: amirta-db-config
                  key: DB_PORT
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: amirta-db-config
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: amirta-db-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: amirta-db-secret
                  key: DB_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: amirta-app-service
spec:
  selector:
    app: amirta-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 80             # Port exposed inside the cluster
      targetPort: 5000     # Flask app inside the pod
      nodePort: 32727      # Port exposed on each node (external access)
